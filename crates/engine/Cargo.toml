[package]
name = "engine"
description = "Pure retirement projection engine (no Tauri dependencies)"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
ts-rs = { workspace = true }
rand = { workspace = true }
rand_distr = { workspace = true }
rayon = { workspace = true }

[dev-dependencies]
# The on-disk plan format, so the migration tests read the YAML shape a
# real plan file carries rather than a JSON approximation of it.
serde_yaml_ng = "0.10"
